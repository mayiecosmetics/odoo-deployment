<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Add Social Conversations button to Contact form -->
    <record id="res_partner_view_form_inherit_social" model="ir.ui.view">
        <field name="name">res.partner.form.social.messenger</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@name='button_box']" position="inside">
                <button name="action_view_social_conversations"
                        type="object" class="oe_stat_button"
                        icon="fa-comments"
                        invisible="social_conversation_count == 0">
                    <field name="social_conversation_count"
                           widget="statinfo"
                           string="Social Chats"/>
                </button>
            </xpath>
            <xpath expr="//field[@name='website']" position="after">
                <field name="social_sender_id" readonly="1"
                       invisible="not social_sender_id"/>
                <field name="social_platform" readonly="1"
                       invisible="not social_platform"/>
            </xpath>
        </field>
    </record>

</odoo>
