<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- ============================================================ -->
    <!-- Social Account: List View                                     -->
    <!-- ============================================================ -->
    <record id="social_account_view_list" model="ir.ui.view">
        <field name="name">social.account.list</field>
        <field name="model">social.account</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="platform"/>
                <field name="page_id"/>
                <field name="state" widget="badge"
                       decoration-success="state == 'connected'"
                       decoration-warning="state == 'draft'"
                       decoration-danger="state == 'error'"/>
                <field name="conversation_count" string="Conversations"/>
                <field name="company_id" groups="base.group_multi_company"/>
            </list>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Social Account: Form View                                     -->
    <!-- ============================================================ -->
    <record id="social_account_view_form" model="ir.ui.view">
        <field name="name">social.account.form</field>
        <field name="model">social.account</field>
        <field name="arch" type="xml">
            <form string="Social Account">
                <header>
                    <button name="action_test_connection"
                            type="object" string="Test Connection"
                            class="oe_highlight"
                            invisible="state == 'connected'"/>
                    <button name="action_test_connection"
                            type="object" string="Re-test Connection"
                            invisible="state != 'connected'"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,connected"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_conversations"
                                type="object" class="oe_stat_button"
                                icon="fa-comments">
                            <field name="conversation_count"
                                   widget="statinfo"
                                   string="Conversations"/>
                        </button>
                    </div>
                    <group>
                        <group string="General">
                            <field name="name"/>
                            <field name="platform"/>
                            <field name="company_id" groups="base.group_multi_company"/>
                        </group>
                        <group string="Meta App Credentials">
                            <field name="app_id"/>
                            <field name="app_secret" password="True"/>
                            <field name="verify_token"/>
                        </group>
                    </group>
                    <group>
                        <group string="Page / Account">
                            <field name="page_access_token" password="True"/>
                            <field name="page_id" readonly="1"/>
                            <field name="instagram_account_id" readonly="1"
                                   invisible="platform != 'instagram'"/>
                        </group>
                        <group string="Webhook">
                            <field name="webhook_url" widget="CopyClipboardChar"/>
                        </group>
                    </group>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- ============================================================ -->
    <!-- Social Account: Action                                        -->
    <!-- ============================================================ -->
    <record id="action_social_account" model="ir.actions.act_window">
        <field name="name">Social Accounts</field>
        <field name="res_model">social.account</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Add your first Social Messenger account
            </p>
            <p>
                Connect your Facebook Page or Instagram Business account
                to receive and reply to messages directly from Odoo.
            </p>
        </field>
    </record>

</odoo>
